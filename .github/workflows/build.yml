on: [push]

name: build

jobs:
  build_and_test:
    name: build and test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: mstksg/setup-stack@v1
      - name: build
        run: stack build
      - name: test
        run: stack test
